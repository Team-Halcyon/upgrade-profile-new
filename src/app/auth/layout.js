export const metadata = {
  title: 'Authentication',
  description: 'Sign in or sign up to your Upgrade Profile account',
};

export default function AuthLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  );
}